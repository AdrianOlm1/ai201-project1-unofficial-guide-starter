# Embedding + vector store (Milestone 4)
# NOTE: the starter pinned `sentence-transformers==3.4.1`, but it requires
# PyTorch, which has no wheel for Python 3.13 on Intel macOS. We instead use the
# SAME model (all-MiniLM-L6-v2) via ChromaDB's built-in ONNX embedding function,
# which runs on onnxruntime (no torch). Same model, same 384-dim embeddings.
chromadb>=0.6.0
onnxruntime>=1.20
groq==0.15.0
python-dotenv==1.0.1

# Ingestion (Milestone 3) — fetch pages and strip HTML to clean text
requests>=2.32
beautifulsoup4>=4.12
lxml>=5.0

# Query interface — add whichever you use for Milestone 5:
# gradio>=6.9.0
# streamlit>=1.40.0

# Document ingestion — add if your documents/ folder contains .pdf files:
# pdfplumber==0.11.4
